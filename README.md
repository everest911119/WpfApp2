# WpfApp2

This is based on winform with mvvm pattern, files name and categorization store in appsetting.json file to make it scalable. mm lenght is editable and it will trigger recalcuate category. If input file have negative or null value, it show error message in MessageBox. Recal Button will save data to json file. Exprot csv will export to csv file. 
